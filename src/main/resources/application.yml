spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/email  # 🔁 Change `your_database_name`
    username: postgres                                    # 🔁 Change `your_username`
    password: password                                    # 🔁 Change `your_password`
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: testingDivyam12@gmail.com
    password: klxfknaeooacnmqp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
